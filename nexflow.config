docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
}

process {
    container = 'hla-typing'
    executor = 'docker'
}

// Executor Configuration
executor {
    name = 'local' // Can be local, slurm, awsbatch, etc., depending on your environment
}